"""内置邮件工作流技能（AGENT_EXTEND_PLAN §2-A7）：方法论提示词包，零规则匹配。

与决策 3（无规则引擎）不冲突——技能是纯提示词层的方法论文档，由模型按需读取，
无任何自动触发/规则匹配代码。系统提示词只注入索引（名字+一句话，prompt 缓存
友好），agent 用 read_skill 工具按需取全文；用户自定义技能（数据目录/设置页）
为后续迭代，先落内置层跑通机制。
"""
from __future__ import annotations

# name → (中文名, 一句话说明, 方法论全文)
BUILTIN_SKILLS: dict[str, tuple[str, str, str]] = {
    "weekly_report": (
        "周报摘要",
        "把本周往来邮件汇总成一封可发送的周报草稿",
        """# 周报摘要技能

目标：产出一份「本周邮件往来周报」草稿（create_draft 进待审，绝不直接发送）。

## 步骤
1. 用 digest_stats 了解当前概况；用 search_emails/list_recent_emails（--date 范围或
   category 过滤）收集本周（用户指定区间，缺省最近 7 天）的往来邮件。
2. 按三类归纳：①需要我行动/已办结的事项 ②收到的重要信息（工作/个人分开）③待回复清单。
3. 草稿结构：开头一句话总览 → 「需行动」「重要信息」「待回复」三节，每节 3-6 条要点，
   每条一行（事项 + 对方 + 简短状态）；结尾给下周关注点。
4. 中文成文、要点式，不写客套话；引用具体邮件时说「发件人+主题」不贴大段原文。
5. 用 create_draft 拟稿进待审，并告诉用户草稿在待审列表。

## 边界
- 不确定区间的先问用户（ask_user）；草稿收件人默认留空由用户自己填。
- 0 封往来的类别如实写「本周无」，不编造。""",
    ),
    "follow_up": (
        "跟进提醒",
        "找出「答应过别人/别人答应过你」但还没兑现的事项",
        """# 跟进提醒技能

目标：扫描邮件往来，整理一份跟进清单——谁答应了什么还没做、你答应了谁还没回。

## 步骤
1. 用 search_emails 找承诺类信号词：日程/承诺/答应/下周/明天/尽快/稍后/回头/确认/
   deadline/expected by 等，配合 unread=false、needs_reply 与日期过滤缩小范围。
2. 逐类甄别（read_email 看上下文，别只看标题）：
   - 对方等你：你说过「明天给你/回头发你/稍后确认」但没有后续；
   - 你等对方：对方承诺「下周给/周五前确认」但已过期无下文。
3. 输出清单：每条 = 事项 | 对方 | 承诺时间 | 已过多久 | 建议动作（催一句/自己补办）。
4. 用户同意后可对需催办的对象 create_draft 拟跟进草稿（一封一主题，进待审）。

## 边界
- 信号词命中≠承诺：拿不准的标注「疑似」，宁缺勿滥；
- 超过 30 天的旧承诺先列出让用户取舍，不自动拟稿。""",
    ),
    "inbox_sweep": (
        "批量归档策略",
        "安全地批量整理收件箱：先搜索分桶、再批量动作",
        """# 批量归档策略技能

目标：把收件箱批量整理干净，同时绝不误动用户可能还要看的邮件。

## 步骤
1. 先用 digest_stats 看收件箱总量与分类分布，向用户一句话报计划再动手。
2. 分桶（全部用 search_emails 结构化过滤拿真实 id，绝不猜 id）：
   - 无争议可直接动：notification/verification/已读的 promo/social → archive_emails 批量归档；
   - 需确认才动：personal/work 未读、带星标、带附件的 → 先列出清单问用户（ask_user）；
   - 黑名单/垃圾类：只建议，trash_emails 前必须向用户说明。
3. 批量动作一次一批（≤50 封），每批汇报结果；失败封数如实转告。
4. 收尾：digest_stats 复查并对比整理前后，给用户一句总结。

## 边界
- 分类来自 AI 自动标记，可能出错——「已读的 work」也先别动，除非用户明确说可以；
- 不做「全部标记已读」这类不可精确回滚的大动作，除非用户点名要求。""",
    ),
    "expense_collect": (
        "报销发票整理",
        "从邮件里收集报销/发票/账单线索并汇总成清单",
        """# 报销发票整理技能

目标：收集指定期间的报销/发票/账单邮件，产出可核对的开支清单。

## 步骤
1. 用 search_emails 搜：发票/报销/账单/订单/支付/收据/invoice/receipt/收件，
   配合 date_from/date_to 与 has_attachments 过滤。
2. read_email 逐封提取：开票方 | 金额（含币种）| 日期 | 用途/项目 | 附件名。
   读不到的（如附件是图片发票）标注「附件待人工核对」，不猜金额。
3. 汇总清单按时间倒序；结尾给合计与「疑似可报销但缺凭证」的提示。
4. 用户需要留档时，把清单拟成 create_draft（发给用户自己或其指定邮箱，进待审）。

## 边界
- 金额与凭证号必须与邮件原文一致，绝不推算或补零；
- 涉及报销政策判断（能不能报）超出能力，只列事实，结论由用户/财务定。""",
    ),
}
